import React from "react";
import Router from "../../routes/Router";

const Layout = () => {
  return (
        <div className="content">
          <Router />
        </div>
  );
};

export default Layout;